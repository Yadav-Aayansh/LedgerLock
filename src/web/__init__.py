"""The web viewer.

An optional second front door onto the same engine. It renders what the
pipeline already produces; it does not decide anything. Every number it shows
comes from `pipeline.execute`, which is the function `run.py` calls too.
"""
